spring:
  application:
    name: masjid-studentski-grad
  
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        jdbc:
          batch_size: 20
          fetch_size: 50
        order_inserts: true
        order_updates: true
  
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB
  
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never
    include-exception: false
  compression:
    enabled: true
    min-response-size: 1024
    mime-types:
      - application/json
      - application/xml
      - text/html
      - text/xml
      - text/plain
      - text/css
      - text/javascript
      - application/javascript

# Application specific properties
app:
  jwt:
    secret: ${JWT_SECRET:myDefaultSecretKeyThatShouldBeChangedInProduction}
    expiration: ${JWT_EXPIRATION:86400000}
    
  stripe:
    api-key: ${STRIPE_API_KEY:}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET:}
    
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://masjid-studentski-grad-pbnx.vercel.app}
  
  prayer-times:
    api-url: https://islamicapi.com/api/v1/prayer-time
    latitude: 42.6977
    longitude: 23.3219
    method: 13
    school: 1
    api-key: ${ISLAMIC_API_KEY:}
    backup-api-url: https://api.aladhan.com/v1/timingsByCity
    city: Sofia
    country: Bulgaria
  
  file-storage:
    upload-dir: ${UPLOAD_DIR:/app/uploads}
  
  frontend-url: ${FRONTEND_URL:https://masjid-studentski-grad-pbnx.vercel.app}
  
  email:
    from: ${EMAIL_FROM:noreply@masjidstudentskigrad.org}
    brevo-api-key: ${BREVO_API_KEY:}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized
      show-components: when-authorized
  metrics:
    export:
      simple:
        enabled: true

logging:
  level:
    root: INFO
    com.masjid: INFO
    org.springframework.security: WARN
    org.springframework.web: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: /app/logs/application.log
    max-size: 10MB
    max-history: 10
